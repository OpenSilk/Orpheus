<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.opensilk.music">

    <application>
        <!-- Local Content Provider-->
        <provider
            android:authorities="org.opensilk.music.provider.debug"
            android:name="com.andrew.apollo.provider.MusicProvider"
            android:exported="false"/>

        <provider
            android:name="org.opensilk.music.artwork.ArtworkProvider"
            android:authorities="org.opensilk.music.artwork.provider.debug"
            android:enabled="true"
            android:exported="true"
            android:grantUriPermissions="true"/>

        <receiver
            android:name="org.opensilk.music.bus.ServiceBroadcastReceiver">
            <intent-filter>
                <action android:name="org.opensilk.music.debug.playstatechanged" />
                <action android:name="org.opensilk.music.debug.metachanged" />
                <action android:name="org.opensilk.music.debug.repeatmodechanged" />
                <action android:name="org.opensilk.music.debug.shufflemodechanged" />
                <action android:name="org.opensilk.music.debug.refresh" />
                <action android:name="org.opensilk.music.debug.queuechanged" />
            </intent-filter>
        </receiver>

    </application>

</manifest>